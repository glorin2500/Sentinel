# Supabase Configuration
# Get these from your Supabase project dashboard: https://supabase.com/dashboard

# ============================================
# CLIENT-SIDE (Safe to expose in browser)
# ============================================
# Project URL (looks like: https://xxxxx.supabase.co)
NEXT_PUBLIC_SUPABASE_URL=your_project_url_here

# Anon/Public Key (long string starting with eyJ...)
# This key is safe to expose - it's protected by Row Level Security (RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ============================================
# SERVER-SIDE ONLY (NEVER expose or commit!)
# ============================================
# Service Role Key - Bypasses RLS, use ONLY in server-side code
# CRITICAL: Never prefix with NEXT_PUBLIC_ or it will be exposed to clients
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# Optional: Custom SMTP (Recommended for production)
# ============================================
# If using custom email provider (SendGrid, AWS SES, etc.)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key
